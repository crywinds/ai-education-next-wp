// Middleware 已禁用，使用簡單的 Context 語系系統
// 如果需要恢復 next-intl，請取消註釋以下代碼並重新配置

/*
import createMiddleware from 'next-intl/middleware'
import { locales, defaultLocale } from './i18n'

export default createMiddleware({
  locales,
  defaultLocale,
  localePrefix: 'always'
})

export const config = {
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)']
}
*/

// 完全禁用 middleware - 返回 undefined 讓 Next.js 跳過
export default function middleware() {
  // 不做任何處理
}

export const config = {
  matcher: []
}
